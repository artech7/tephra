# user data — inert with run.py, essential under docker compose
vault/

# run.py bootstrap (.tephra-deps stamp lives inside)
.venv/

# packaging/build_macos.sh and build_windows.ps1's disposable build env
.venv-build/

# python
__pycache__/
*.py[cod]

# desktop builds
build/
dist/

# macOS
.DS_Store

# node (jsdom for the ui_*.mjs suites)
node_modules/

# local docker-compose env and config-dir mount
.env
config/
